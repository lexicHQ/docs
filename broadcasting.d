# Broadcasting (Push Notifications)

Broacasting let you send engage with your customers and giving chatbots and excellent vehicle to send out promotions and offers.

You can send broadcast in the following methods:

    * Using JSON API Plugin
    * Using User Interface (Comming Soon)


## Using JSON API Plugin

It is possible to use JSON API to send out broadcasts to your subscribers. Below is an example of sending broadcast using the `script` block. It is using `recime-bot-extension` and `request` module to send your broadcast.

